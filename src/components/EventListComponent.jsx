// import WideEventCard from "./WideEventCard.jsx";
//
// const EventListComponent = ({ events }) => {
//     return (
//         <div className="w-3/4 space-y-6">
//             {events.length > 0 ? (
//                 events.map((event) => (
//                     <WideEventCard key={event.id} event={event} />
//                 ))
//             ) : (
//                 <p>No events found.</p>
//             )}
//         </div>
//     );
// };
//
// export default EventListComponent;
